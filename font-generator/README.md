# Python字体生成服务

本目录包含用于生成字体文件的Python脚本。

## 安装依赖

```bash
pip install -r requirements.txt
```

## 使用方法

```bash
python generator.py --spec <规格JSON文件> --output <输出目录> --font-id <字体ID>
```

## 文件说明

- `generator.py`: 字体生成主程序
- `spec_parser.py`: 规格解析工具
- `requirements.txt`: Python依赖列表








